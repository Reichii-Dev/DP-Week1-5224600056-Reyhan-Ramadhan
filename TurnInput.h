#pragma once

// ============================================================
//  TurnInput — Data hasil generate input untuk satu ronde
// ============================================================
struct TurnInput {
    int value;  // nilai input (misal: angka kartu)
};
